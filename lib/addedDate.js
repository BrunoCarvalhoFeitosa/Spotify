export function formatDateAddedAt(addedAt) {
    console.log('addedAt', addedAt);
  
    const completeDate = addedAt?.split('-');
    // const day = completeDate[2].substring(0, 2);
    // const month = completeDate[1];
    // const year = completeDate[0];
    // const finalDate = day + month + year;
  
    // return finalDate;
  }